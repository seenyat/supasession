import React from 'react';
import { useLyricsPlus } from '../../../utils/hooks';

const Queue = () => {
  // useLyricsPlus();
  return (
    <div
      style={{ scrollbarWidth: 'none' }}
      className="relative"
    ></div>
  );
};

export default Queue;
